import { useTranslation } from "react-i18next";
import "./Result.css";

export const Result = () => {
  const { t } = useTranslation();

  return (
    <section id="result">
      <div></div>
    </section>
  );
};
